{
    "title": "LinkedIn Profile Posts Scraper",
    "description": "Scrapes posts from LinkedIn profiles",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "username": {
            "title": "LinkedIn Username",
            "type": "string",
            "description": "Your LinkedIn account username/email",
            "editor": "textfield"
        },
        "password": {
            "title": "LinkedIn Password",
            "type": "string",
            "description": "Your LinkedIn account password",
            "editor": "textfield",
            "isSecret": true
        },
        "profileUrls": {
            "title": "Profile URLs",
            "type": "array",
            "description": "List of LinkedIn profile URLs to scrape",
            "editor": "stringList",
            "uniqueItems": true,
            "minItems": 1
        },
        "maxPosts": {
            "title": "Maximum Posts",
            "type": "integer",
            "description": "Maximum number of posts to scrape (0 for unlimited)",
            "minimum": 0,
            "default": 0,
            "nullable": true
        },
        "useProxy": {
            "title": "Use Proxy",
            "type": "boolean",
            "description": "Whether to use a proxy for scraping",
            "default": false
        },
        "includeComments": {
            "title": "Include Comments",
            "type": "boolean",
            "description": "Whether to scrape comments for each post",
            "editor": "checkbox",
            "default": false
        },
        "maxCommentsPerPost": {
            "title": "Maximum Comments per Post",
            "type": "integer",
            "description": "Maximum number of comments to scrape per post (if comments are enabled)",
            "editor": "number",
            "default": 10,
            "minimum": 1,
            "maximum": 100
        },
        "dateFrom": {
            "title": "Start Date",
            "type": "string",
            "description": "Only include posts from this date (format: YYYY-MM-DD)",
            "editor": "textfield",
            "nullable": true
        },
        "dateTo": {
            "title": "End Date",
            "type": "string",
            "description": "Only include posts until this date (format: YYYY-MM-DD)",
            "editor": "textfield",
            "nullable": true
        },
        "includeMedia": {
            "title": "Include Media",
            "type": "boolean",
            "description": "Whether to extract media URLs (images, videos) from posts",
            "editor": "checkbox",
            "default": false
        }
    },
    "required": ["username", "password", "profileUrls"]
}